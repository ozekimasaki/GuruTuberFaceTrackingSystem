---
trigger: always_on
---
# プロジェクトルール

- 作業前に必ず `AGENTS.md` を読み、プロジェクト構成・規約・アーキテクチャに従うこと
- コメントは日本語で記述すること
- ファイル名はケバブケースを使用すること
- スタイルはインライン `style` オブジェクトを使用し、CSS Modules や Tailwind は導入しないこと
- 関数コンポーネント + Hooks のみを使用し、クラスコンポーネントは使用しないこと
